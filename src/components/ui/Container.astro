---
interface Props {
  class?: string;
  as?: keyof astroHTML.JSX.IntrinsicElements;
}

const { class: className, as: Tag = "div", ...rest } = Astro.props;
---

<Tag class=`container ${className || ""}` } {...rest}>
  <slot />
</Tag>
